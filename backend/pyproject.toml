[project]
name = "urbanlens-backend"
version = "0.1.0"
description = "FastAPI backend for the UrbanLens-YYC MASIV prototype"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
